<%= form_for(@mirror) do |f| %>
  <% if @mirror.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mirror.errors.count, "error") %> prohibited this mirror from being saved:</h2>

      <ul>
      <% @mirror.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uri %><br />
    <%= f.text_field :uri %>
  </div>
  <div class="field">
    <%= f.label :slug %><br />
    <%= f.text_field :slug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
